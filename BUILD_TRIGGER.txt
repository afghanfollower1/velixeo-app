Trigger signed Android build - VELIXEO v0.9.5 - 2026-09-21.
Release scope:
1) Smart Buy now follows the 5SIM-style service-only flow and automatically selects the best live country/operator.
2) Virtual Numbers admin Services loads the complete synchronized 5SIM catalog instead of stopping at 1000 rows.
3) Service search therefore finds services that were previously visible in the app but missing from the admin list.
4) Social provider creation was hardened: slug stays optional/auto-unique and a provider can be saved before its API key is added.
5) Provider currency is now a dropdown instead of free text, with AFN/USD/TOMAN and configured provider currencies available.
6) Provider currency selection is saved to the real Provider record and used by AFN pricing conversion.
7) AFN remains the accounting/base currency; customers may display AFN, USD or Toman.
8) Full signed APK update is used for this development-stage release.
9) Backend and Flutter validations must pass before release publication.
