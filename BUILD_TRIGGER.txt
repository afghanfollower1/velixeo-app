Trigger signed Android build - VELIXEO v0.9.4 - 2026-09-21.
Release scope:
1) Virtual Numbers admin Services now has per-service icon upload next to every service.
2) Uploaded icons are compressed in-browser to lightweight 128x128 WebP before storage.
3) Mobile catalog receives only a tiny cached icon URL; it never downloads icon blobs inside the catalog JSON.
4) Uploaded admin icon overrides built-in brand icon; failures fall back to existing brand icon/initials.
5) Admin can set Display order per virtual-number service (1, 2, 3, ...).
6) App service picker follows the exact backend/admin sortOrder instead of hardcoded popularity sorting.
7) Future 5SIM syncs preserve admin icon and manual display order.
8) Virtual Numbers remains isolated from Social Media.
9) Backend tests and Flutter validation passed before build trigger.
