name: velixeo
publish_to: 'none'
description: VELIXEO bilingual digital services marketplace.
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
